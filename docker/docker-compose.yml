version: '2'
services:
        homeassistant:
                image: homeassistant
                build: homeassistant/
                network_mode: "host"
                volumes:
                        - /etc/localtime:/etc/localtime:ro
                        - /etc/homeassistant:/config
